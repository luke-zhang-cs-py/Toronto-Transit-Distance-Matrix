__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.DS_Store

# Coverage output. A binary database of one local run -- it was committed
# by accident and changes on every test invocation.
.coverage
htmlcov/
.pytest_cache/

# Built from TTC static GTFS by tools_build_schedule.py. Derived data, ~0.6 MB,
# and it goes stale when the board period changes -- rebuild rather than pull.
schedule_index.json
_gtfs.zip
