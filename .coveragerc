[run]
omit =
    tests/*
    # A one-shot build tool: it streams a 207 MB GTFS archive and writes
    # an index. Exercising it in the suite would mean shipping the archive.
    tools_build_schedule.py
